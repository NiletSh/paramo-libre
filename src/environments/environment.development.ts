export const environment = {
  production: false,
  apiUrl: '/api',
  appName: 'Páramo Libre',
  siteUrl: typeof window !== 'undefined' ? window.location.origin : 'http://localhost:4200',
  version: '1.0.0'
};
